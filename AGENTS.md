# AGENTS.md — قواعد العمل على هذا المستودع

<div dir="rtl">

## نظرة عامة

التطبيق مشروع **Next.js 16 + TypeScript + Tailwind CSS 4** بتصدير ثابت (`output: 'export'`). مساران: `/` (الفهرس) و `/quiz/` (اختبار بصمتك العطرية). مدير الحزم: **pnpm** حصراً.

الملف `perfumes.html` (جذر المستودع) هو **النسخة القديمة أحادية الملف — أوراكل مرجعي للتوافق فقط**: لا تُطوِّر ميزات جديدة فيه، ولا تُعدّله إلا لإصلاح موثّق. كل التطوير في `src/`.

اللغة الافتراضية: **العربية (RTL)**. جميع النصوص الظاهرة بالعربية، والأرقام عربية-هندية عبر `arN()`.

## الأوامر

```bash
pnpm install        # تثبيت الاعتمادات
pnpm dev            # تطوير على http://localhost:3000
pnpm build          # بناء + تصدير ثابت إلى out/
pnpm typecheck      # فحص الأنواع (tsc --noEmit)
pnpm lint           # ESLint
npx serve out -l 3001   # أو python3 -m http.server 3001 -d out — معاينة النسخة الثابتة
```

## القواعد الذهبية (لا تكسرها)

1. **pnpm فقط.** لا تستخدم npm ولا yarn.
2. **لا SSR/API:** التصدير ثابت بالكامل — لا مسارات ديناميكية، لا قواعد بيانات، لا مصادقة. المكونات التفاعلية تبدأ بـ `'use client'`.
3. **احفظ UTF-8** وإلا تتعطّل العربية.
4. **RTL أولاً.** خصائص منطقية (`inset-inline-start`, `margin-inline-start`) لا `left/right`. وسم `<html dir="rtl">` في `src/app/layout.tsx` مقدّس.
5. **أرقام عربية.** كل رقم يُعرض للمستخدم يمر عبر `arN()` (والكسور عبر `arDec()` في `page.tsx`). لا أرقام لاتينية في النصوص الظاهرة.
6. **حافظ على ثوابت الحالة** — جزء من العقد العام مع النسخة القديمة:
   - `localStorage['p100favs']` → مصفوفة **فهارس** بطاقات (ترتيب `rawPerfumes` لا يتغيّر).
   - وسائط الرابط `#`: `cat`, `q`, `blind`, `sort`, `fav`, `brand`, `occ`, `note` (انظر `src/lib/catalog/state.ts`).
   لا تُغيّر أسماءها دون توافق رجعي.
7. **لا تكسر تكافؤ الأوراكل:** أي تعديل على منطق الفلترة/الترتيب/التشابه/الاختبار يجب أن يُطابق `perfumes.html` (راجع قسم التحقق).

## خريطة الكود

| المسار | الوظيفة |
|---|---|
| `src/app/layout.tsx` | الجذر: RTL، الميتا، الفافيكون |
| `src/app/globals.css` | خطوط محلية (`public/fonts/` مع بديل Google)، متغيرات الألوان، كل أنماط الواجهة (منقولة من القديم بأسمائها: `.card`/`.stage`/`.toolbar`/`.fpanel`…) |
| `src/app/page.tsx` | صفحة الفهرس: الترويسة، شريط الأدوات، الأقسام، بطاقات العطور، موسوعة النوتات |
| `src/app/quiz/page.tsx` | صفحة الاختبار المستقلة (٦ أسئلة + النتائج) |
| `src/domain/types.ts` | الأنواع: `Perfume`, `Category`, `FilterState`, `Occasion`, `SortMode` |
| `src/data/perfumes.ts` | `CATS` (١١ عائلة)، `rawPerfumes` (**٥٦٩ سجلاً**)، `applyBoozy` |
| `src/lib/arabic.ts` | `arN`/`norm`/`normAr`/`normEn`/`priceRank` — المصدر الموحّد الوحيد |
| `src/lib/catalog/` | `filter`/`sort`/`similarity`/`occasions`/`notes`/`state` — منطق نقيّ بلا DOM |
| `src/lib/quiz/` | `questions`/`scoring` — منطق الاختبار |
| `src/hooks/` | `useCatalogState` (الحالة + مزامنة الرابط `#`)، `useFavorites` (localStorage) |
| `src/components/catalog/Bottle.tsx` | `BottleBig`/`BottleMini` — قوارير SVG (٥ أشكال/أغطية) |
| `next.config.ts` | `output:'export'`, `trailingSlash:true`, `images.unoptimized` |

## نموذج البيانات

سجل العطر في `src/data/perfumes.ts` كائن مسمّى الحقول (لا وسيطي مثل `D()` القديم):

```ts
{ c: "familyKey", n: "الاسم العربي", en: "English Name", br: "Brand", bar: "الدار بالعربية",
  notes: ["نوتة", …], an: "البديل", abr: "Alt Brand", abar: "البديل بالعربية", anotes: […],
  pf: 74, ps: 66,          // ثبات وفوحان الأصلي (٠–١٠٠)
  af: 80, as: 72,          // ثبات وفوحان البديل
  bl: true, bla: true,     // أمان الشراء الأعمى (أصلي/بديل)
  rate: 4.4, price: "$$" } // التقييم وشرائح السعر
```

**إلزامي عند الإضافة:** أضِف السجل في نهاية `rawPerfumes` (الفهارس مرتبطة بالمفضّلة والروابط). الحقول المحسوبة (`occ` وسوم المناسبات، `sim` درجة التشابه) تُملأ تلقائياً بـ `applyOccasions`/`assignScores` — لا تكتبها يدوياً.

## القواعد التقنية

- **التهيئة في الصفحات** بهذا الترتيب (لا تدمج الاستدعاءات — `applyBoozy` تُعيد مصفوفة جديدة بينما الباقي يغيّر في المكان):
  ```ts
  const DATA = applyBoozy(rawPerfumes);
  applyOccasions(DATA);
  assignScores(DATA);
  ```
- **التطبيع العربي للبحث:** `norm` يزيل التشكيل ويوحّد ألف/إ/آ→ا، ة→ه، ى→ي. أي حقل نصي جديد تريد البحث فيه أضِفه إلى `buildHaystacks` في `src/lib/catalog/filter.ts`. `normAr` (مطبيع مفاتيح بلا أحرف صغيرة) دالة مختلفة — لا تدمجهما.
- **درجات التشابه:** `similarity.ts` يضيف `d.sim` (٠–١٠٠) من قاموس `SIM` (المفتاح = اسم البديل بعد `normAr`) مع تجاوزات `OVR`. الشارة تُبنى في `alt-head` داخل البطاقة (`sim-twin`/`sim-strong`/`sim-close`/`sim-vibe`).
- **الاختبار:** عائلات Q2 تُقرأ من الخانة `answers[1]` (مصفوفة متعددة) — لا تُفلتر بالمفتاح المسطّح لأن مفاتيح نوتات Q4 (`citrus`) تتطابق مع مفاتيح `CATS`.
- **الأنماط:** أنظمة مزدوجة — Tailwind للاختبار والصفحات الخفيفة، وأصناف القديم المنقولة حرفياً في `globals.css` للفهرس (التكافؤ البصري). لا تُعِد تسمية أصناف القديم.
- **القوارير بلا صور:** SVG مولّدة داخل `Bottle.tsx` فقط. لا صور نقطية.
- **`prefers-reduced-motion`:** احترم قاعدة الإلغاء في نهاية `globals.css` عند أي حركة جديدة.
- **الخطوط محلية** في `public/fonts/` (١٤ ملف woff2) مع بديل Google Fonts في `@font-face` — لا تُضف خطوطاً عبر CDN آخر.

## التحقق قبل التسليم

1. `pnpm typecheck && pnpm lint && pnpm build` — يجب أن تنجح جميعها.
2. **توافق الأوراكل** (إذا عدّلت بيانات أو منطقاً): قارن النتائج مع `perfumes.html` — عدد السجلات، نتائج بحث مطابقة (عود/فانيليا/لطافة/Creed)، ترتيب الأوضاع الستة، درجات التشابه، مسارات الاختبار الخمسة.
3. معاينة النسخة الثابتة: `pnpm build` ثم `npx serve out -l 3001` — جرّب `/` و `/quiz/` و`404`.
4. يدوياً في المتصفح: بحث عربي مع/بدن تشكيل، فلترة عائلة/دار/نوتة/مناسبة، زر الشراء الأعمى، المفضّلة، كل خيارات الترتيب، إعادة تحميل بعد مشاركة رابط بحالة، موسوعة النوتات.
5. على عرض هاتف (≤٧٦٠px): زر «⚙ فلترة» يفتح اللوحة وتُغلق بـ✕/`Esc`/النقر خارجها.
6. الاختبار: زر «✨ اكتشف عطرك المثالي» في الترويسة، الإجابة على ٦ أسئلة، «شاهده» يقفز إلى البطاقة `#p-N`.

## النشر

- الإنتاج: **Hostinger** — ارفع محتويات `out/` إلى `public_html`.
- Vercel للمعاينة/CI فقط (البناء يعمل دون تهيئة إضافية).

## Git / PR

- رسالة التزام واضحة بالعربية أو الإنجليزية، فرع مُسمّى وصفياً (`add/perfume-xyz`, `fix/search-normalize`, `docs/…`).
- لا تدمج قبل إكمال التحقق أعلاه.
- عند دمج PR لا تحذف الفرع.

</div>
