# AGENTS.md — قواعد العمل على هذا المستودع

<div dir="rtl">

## نظرة عامة

التطبيق **ملف واحد**: `perfumes.html` يحتوي كل الـ HTML و CSS و JavaScript. لا يوجد `package.json`، ولا خطوة بناء، ولا معالج، ولا اعتماد npm، ولا إطار عمل. الهدف: ملف يُفتح مباشرة في المتصفح ويعمل.

اللغة الافتراضية: **العربية (RTL)**. جميع النصوص الظاهرة للمستخدم بالعربية، والأرقام عربية-هندية عبر `arN()`.

## القواعد الذهبية (لا تكسرها)

1. **ملف واحد فقط.** لا تُنشئ ملفات `.js`/`.css` منفصلة ولا تستورِد خارجياً. كل شيء يبقى داخل `perfumes.html`.
2. **بلا اعتمادات.** لا تضِف مكتبات (jQuery / React / Tailwind…). JS أصيل (Vanilla) فقط. الخطوط من Google Fonts هي الاستثناء الوحيد للاتصال بالشبكة.
3. **احفظ UTF-8.** أي تعديل يجب أن يُحفظ بترميز UTF-8 وإلا تتعطّل العربية وتظهر رسالة الخطأ الحمراء.
4. **لا تكسر الغلاف الآمن.** كل منطق الـ JS داخل `try{ … }catch(err){ … }` (`perfumes.html:274` و `1221`). أي خطأ يُظهر شريط `errbar` للمستخدم بدل شاشة بيضاء. أبقِ هذا النمط.
5. **RTL أولاً.** استخدم خصائص منطقية مثل `inset-inline-start` و `margin-inline-start` لا `left/right`. وسم `<html dir="rtl">` مقدّس.
6. **أرقام عربية.** كل رقم يُعرض للمستخدم يمر عبر `arN()`. لا تضع أرقاماً لاتينية ثابتة في النصوص الظاهرة.
7. **حافظ على ثوابت الحالة.** مفاتيح `localStorage` ووسائط رابط الصفحة `#` جزء من العقد العام:
   - `localStorage['p100favs']` → مصفوفة فهارس المفضّلة.
   - وسائط الرابط: `cat`, `q`, `blind`, `sort`, `fav`, `brand` (انظر `saveState`/`loadState`).
   لا تُغيّر أسماءها دون توافق رجعي، وإلا تنكسر الروابط المحفوظة.

## خريطة الكود (تقريبية — الأرقام تنزلق مع كل تعديل؛ اعتبر أسماء الوحدات/DOM هي المرجع)

| المنطقة | الأسطر | الوظيفة |
|---|---|---|
| `:root` + الستايل | 21 – 202 | المتغيرات اللونية والتنسيق الكامل (شارات التشابه `196–201`) |
| `<body>` الهيكل | 204 – 272 | الهيدر، الترويسة، شريط الأدوات، `main`، الفوتر |
| `try{` + `App` + `App.Util` | 274 – 289 | مساحة الاسم `App` والمساعدات الموحّدة `arN`/`norm`/`normAr`/`priceRank` |
| `CATS` (العائلات) | 290 – 302 | `{ key: { n, d, c, c2 } }` |
| `D(...)` + `data` | 303 – 858 | مُنشئ سجل العطر ومصفوفة العطور |
| `BOOZY` + `favs` | 859 – 861 | ترحيل البوزية + مجموعة المفضّلة (localStorage) |
| `App.Bottle` | 877 – 903 | `big`/`mini` — مولّدات SVG للقوارير (خمسة أشكال) |
| `App.Sim` | 906 – 939 | `SIM`/`scoreOf`/`tierOf`/`assignScores` (يكتب `d.sim` لكل سجل) |
| `App.Store` | 942 – 948 | مصدر الحالة الموحّد (`curCat`) |
| `App.Render` | 951 – 1016 | `card`/`sections`/`stats`/`records`/`jsonld`/`masthead`/`simBadge` (دوال نقيّة) |
| `App.Filter` | 1019 – 1031 | `matches`/`hay` — منطق فلترة نقيّ بلا DOM |
| `App.Sort` | 1034 – 1048 | `by` — منطق ترتيب نقيّ بلا DOM |
| `App.Router` | 1051 – 1074 | `saveState`/`loadState`/`syncChips` (مزامنة الرابط `#`) |
| `App.Dom` | 1077 – 1139 | `applyView`/`renderChrome`/`injectSimBadges`/`updateFavCount` — الوحدة الوحيدة التي تُعدّل DOM |
| `App.Boot` | 1142 – 1217 | `buildToolbar`/`wireEvents`/`initObservers`/`init` + `openSheet`/`closeSheet` (تنسيق فقط) |
| `App.Boot.init()` | 1219 | نقطة الإقلاع |
| `}catch(err)` | 1221 | الغلاف الآمن `errbar` |

## شريط الأدوات (متجاوب)

شريط الأدوات `.toolbar` (لاصق `top:0`) يتكيّف بين الحواسيب والجوال عبر CSS فقط (لا نقلّ DOM):

- **الحواسيب (>٧٦٠px):** الفلاتر `#blindOnly`/`#sort`/`#brand` ظاهرة **inline** داخل `.trow`، لأنّ غلافها `.fpanel{display:contents}` (أبناءه يُصبحون عناصر `.trow` مباشرة). زرّ `.fbadge` مخفيّ.
- **الجوال (≤٧٦٠px):** `.trow` = حقل البحث + زرّ `.fbadge` فقط (شريط نحيف). الفلاتر داخل `.fpanel` مخفية (`display:none`) وتُكشَف **كلوحة منسدلة** (`flex:1 1 100%`) عند لمس `.fbadge`، بتبديل الصنف `.open` عبر `openSheet()`/`closeSheet()` في `App.Boot`. تُغلق بـ✕ أو `Esc` أو النقر خارجها.

**تحذير — لا تخلط بين الصنفين:**
- `.sec` مخصّص **لأقسام العائلات** (`<section class="sec" id="sec-…">`) فقط، وتستهدفه `hideEmptySections` عبر `querySelectorAll('.sec')`.
- `.fpanel` هو **لوحة الفلاتر** (`id="sec"`). إعادة استخدام `.sec` للوحة الفلاتر تكسر تنسيق الأقسام وتُخفي اللوحة قسراً (سبق وأنشأ هذا خللاً).

**الإخراج الجانبي للعرض (`App.Dom.renderChrome(s)`):** تُستدعى من `applyView` وتُحدّث:
- رقائق الفلاتر النشطة (`.active`/`.achip` — قابلة للإزالة، `data-clear="blind|brand"`).
- شارة عدّاد `.fbadge`/`.fdot` (عدد الفلاتر النشطة بالأرقام العربية عبر `arN()`).
- «أثر البحث» (المتغير `--qfill` على `.search` + زرّ المسح `#qclear`).

**خريطة بيانات مساعدة:** `brandBar` (مفتاح الدار `br` → الاسم العربي) تُعبَّأ في `buildToolbar` وتُستهلك في `renderChrome`.

## نموذج البيانات

سجل العطر يُنشأ بـ `D(...)` بهذا الترتيب بالضبط:

```
D( categoryKey,
   nameAr, nameEn, brandEn, brandAr,
   [ notesOriginal ],
   altNameAr, altBrandEn, altBrandAr,
   [ notesAlt ],
   longevityOrig, sillageOrig,
   longevityAlt, sillageAlt,
   blindSafeOrig (bool), blindSafeAlt (bool),
   rating (e.g. 4.7), priceTier ('$'|'$$'|'$$$') )
```

عند إضافة عطر، اتبع ترتيب الوسائط حرفياً؛ أي إزاحة ستُفسَّر كقيمة خاطئة (مثلاً نسبة مئوية كحكم شراء أعمى).

## القواعد التقنية

- **ES5-ish:** الكود يستخدم `var` ودوال تعبيرية وتجنّب ميزات ES6 الحديثة عمداً (توافق أوسع). حافظ على هذا النمط.
- **البنية المعيارية:** كل المنطق داخل وحدات IIFE تحت مساحة الاسم `App` (`Util`/`Bottle`/`Sim`/`Store`/`Render`/`Filter`/`Sort`/`Router`/`Dom`/`Boot`)، كلٌّ بمسؤولية واحدة وواجهة دنيا، والتدفق نازلٌ بلا دورات. المساعدات `arN`/`norm`/`normAr`/`priceRank` مصدرها الوحيد `App.Util` (لا تُكرّرها). المنطق النقي (`Filter`/`Sort`) بلا وصول للـ DOM؛ والآثار الجانبية محصورة في `App.Dom` وحدها.
- **التطبيع العربي للبحث:** `App.Util.norm` يزيل التشكيل ويوحّد ألف/إ/آ→ا، ة→ه، ى→ي (ويُحوّل للأحرف الصغيرة). أي حقل نصي جديد تريد البحث فيه أضِفه إلى `hay` داخل `App.Filter`. لاحظ أن `App.Util.normAr` (مطبيع المفاتيح، بلا أحرف صغيرة ويطوي المسافات) دالة مختلفة عن `norm` — لا تدمجهما.
- **درجات التشابه:** وحدة `App.Sim` (`906–939`) تضيف `d.sim` (٠–١٠٠) لكل سجل من قاموس `SIM` (المفتاح = اسم البديل بعد `normAr`)، ووحدة `App.Render.simBadge` تبني الشارة. يُعاد الحقن تلقائياً بعد أي فرز/فلترة عبر `App.Dom.injectSimBadges` بفضل `MutationObserver` على `#main` (يضعه `App.Boot.initObservers`). لا تُحقن الشارات يدوياً داخل `card()`.
- **القوارير بلا صور:** كل قارورة SVG مولّدة من شكل (`shapes[]`) وغطاء (`caps[]`) ولوني العائلة داخل `App.Bottle`. لا تضِف صوراً نقطية.
- **الكشف + التحميل الكسول:** لا تُحقن SVG القارورة إلا عند ظهور البطاقة (داخل `App.Boot.initObservers`). لا تُحقن كل العطور دفعة واحدة.
- **`prefers-reduced-motion`:** احترم قاعدة الإلغاء في `194` عند أي حركة جديدة.

## التحقق قبل التسليم

- افتح `perfumes.html` محلياً.
- تأكّد في console من: `✓ تم تحميل العطور بنجاح` وعدم ظهور `errbar` أحمر.
- جرّب: بحث عربي مع/بدن تشكيل، فلترة عائلة، فلتر الدار، زر الشراء الأعمى، المفضّلة، الترتيب بكل خيار، إعادة تحميل الصفحة بعد مشاركة رابط بحالة.
- على عرض هاتف (≤٧٦٠px): تأكّد من ظهور زرّ «⚙ فلترة»، وانسدال لوحة الفلاتر وإغلاقها (✕ / `Esc` / النقر خارجها)، وظهور رقائق الفلاتر النشطة القابلة للإزالة فوق رقائق العائلات، وبقاء أقسام العائلات بتنسيقها الطبيعي.
- تأكّد من ظهور شارات درجة التشابه على البطاقات (٤ طبقات لونية) وبقائها بعد الفرز/الفلترة دون تكرار.
- لا يوجد اختبار آلي ولا lint: الفحص يدوي عبر المتصفح.

## Git / PR

- رسالة التزام واضحة بالعربية أو الإنجليزية.
- فرع مُسمّى وصفياً (مثل `add/perfume-xyz` أو `fix/search-normalize`).
- لا ترفع `perfumes.html` مُخزّناً بترميز غير UTF-8.
- لا تدمج الفرع قبل التحقق اليدوي أعلاه.

</div>
