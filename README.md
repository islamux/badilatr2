<div dir="rtl">

# موسوعة المئتين · أفضل ٢٠٠ عطر رجالي وبدائلها

> دليل تفاعلي واحد يضم **٢٠٠ عطر رجالي** من نخبة التقييمات العالمية، إلى جانب **البديل العربي/الاقتصادي** الموثوق لكل قارورة — مع النوتات العطرية، نسب الثبات والفوحان، وحكم نهائي للشراء الأعمى. مصنّف في **عشر عائلات عطرية** وبحث وفلترة فورية.

ملف واحد (`perfumes.html`) لا يحتاج خادماً ولا خطوة بناء ولا أي اعتماد خارجي — افتحه في المتصفح فقط، ويعمل دون إنترنت بعد التحميل الأول للخطوط.

</div>

<div dir="rtl">

## ✨ المزايا

- **مئتا عطر** مُقيَّمة، لكلٍّ منها بديل مُدروس (اسم + دار + نوتات).
- **عشر عائلات عطرية** ملوّنة: خشبية، شرقية عنبرية، حمضية، أروماتيك، مائية، جلدية وتبغ، حارة، زهرية رجالية، جورماند، فوجير.
- **قوارير SVG مُولَّدة برمجياً** بخمسة أشكال وألوان لكل عائلة — لا صور، لا أصول، حجم صغير.
- **بحث فوري** مع تطبيع الحروف العربية (التشكيل، ألف/إ/آ، ة/ه، ى/ي، المدّة).
- **فلترة** بالعائلة + زر «بدائل آمنة للشراء الأعمى فقط» + **المفضّلة**.
- **ترتيب**: الافتراضي / الأعلى تقييماً / الأطول ثباتاً / الأقوى فوحاناً / السعر / أبجدياً.
- **حفظ الحالة في رابط الصفحة** (`#cat=...&q=...&blind=1&sort=...&fav=1`) — مشاركة نتيجة بحث محدّدة.
- **مفضّلة محفوظة** في المتصفح (`localStorage`).
- **بيانات منظمة JSON-LD** (`ItemList` من `Product`) لمحركات البحث.
- ظهور تدريجي عند التمرير، شريط تقدّم، زر العودة للأعلى، ودعم `prefers-reduced-motion`.
- واجهة **RTL** بترميز **UTF-8** وأرقام عربية-هندية (٠١٢٣…).

## 🚀 التشغيل

```bash
# لا حاجة لتثبيت أي شيء — افتح الملف مباشرة:
open perfumes.html        # macOS
xdg-open perfumes.html    # لينكس
start perfumes.html       # ويندوز
```

أو انشره على أي استضافة ثابتة (GitHub Pages / Netlify / Cloudflare Pages) برفع `perfumes.html` فقط.

> ملاحظة: الخطوط (`Aref Ruqaa`, `Tajawal`) تُجلب من Google Fonts؛ وباقي العمل يعمل دون اتصال.

## 🗂️ البنية

```
perfumes.html   ← التطبيق كاملاً (HTML + CSS + JS) في ملف واحد
```

| القسم داخل الملف | التقريبي | المحتوى |
|---|---|---|
| `<head>` | 1 – 152 | وصف، Open Graph، الستايل (`<style>`) |
| الهيكل (`<body>`) | 153 – 211 | الهيدر، الترويسة، شريط الأدوات، `<main>`، الفوتر |
| `<script>` | 212 – 619 | البيانات + منطق العرض والفلترة |

## 🧪 نموذج البيانات

كل عطر يُنشأ عبر الدالة المساعدة `D(...)` بهذا الترتيب:

```
D(العائلة,
  الاسم_عربي, الاسم_إنجليزي, الدار_إنجليزي, الدار_عربي,
  [نوتات_الأصلي],
  البديل_عربي, دار_البديل_إنجليزي, دار_البديل_عربي,
  [نوتات_البديل],
  ثبات_الأصلي٪, فوحان_الأصلي٪,
  ثبات_البديل٪, فوحان_البديل٪,
  أمان_الشراء_الأعمى_للأصلي (true|false),
  أمان_الشراء_الأعمى_للبديل (true|false),
  التقييم (4.x), فئة_السعر ('$'|'$$'|'$$$'))
```

والعائلات معرّفة في الكائن `CATS` بالمفتاح → `{ n:الاسم, d:الوصف, c:اللون, c2:اللون_الداكن }`.

### العائلات العشر

| المفتاح | الاسم |
|---|---|
| `woody` | خشبية |
| `oriental` | شرقية عنبرية |
| `citrus` | حمضية منعشة |
| `aromatic` | أروماتيك |
| `aquatic` | مائية بحرية |
| `leather` | جلدية وتبغ |
| `spicy` | حارة توابل |
| `floral` | زهرية رجالية |
| `gourmand` | جورماند حلوة |
| `fougere` | فوجير كلاسيكي |

## 🛠️ التخصيص السريع

- **إضافة عطر:** أضف سطر `D(...)` داخل مصفوفة `data` (`perfumes.html:228`) ضمن دفعة عائلته.
- **إضافة عائلة:** أضف مفتاحاً إلى `CATS` (`perfumes.html:215`) بلون مميّز.
- **الألوان العامة:** متغيرات `:root` في الأعلى (`--gold`, `--ink`, `--bg` …).
- **التشفير:** احفظ الملف دائماً بترميز **UTF-8** وإلا تتعطّل العربية.

## 🤝 المساهمة

1. أنشئ فرعاً (مثل `add/perfume-xyz`).
2. أضِف/عدّل سطر `D(...)` واحفظ UTF-8.
3. افتح `perfumes.html` وتأكّد من ظهور رسالة الوحدة في الطرفية:
   `✓ تم تحميل ٢٠٠ عطرًا بنجاح`
4. افتح Pull Request.

راجع [AGENTS.md](./AGENTS.md) للقواعد التقنية المفصّلة عند التعديل.

## 📄 الترخيص

انظر ملف [LICENSE](./LICENSE).

</div>

---

<div dir="ltr">

> **EN — TL;DR:** A single self-contained `perfumes.html` (no server, no build, no dependencies) listing the top **200 men's perfumes**, each paired with a vetted Arabic/economic **alternative**, plus notes, longevity/sillage scores, blind-buy verdicts, instant Arabic-normalized search, family filters, sorting, favorites, and shareable URL state. UI is Arabic RTL. See [AGENTS.md](./AGENTS.md) for the engineering rules before editing.

</div>
